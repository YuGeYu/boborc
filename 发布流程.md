# 发布流程

这份文档用于保证这个项目以后可以稳定地：

- 在本地修改后提交到 GitHub
- 在需要时一键部署到 Cloudflare Pages

## 1. 当前已确认可用的环境

- Git 远程仓库：`origin -> https://github.com/YuGeYu/boborc.git`
- 当前默认分支：`main`
- GitHub 推送：已验证成功
- Cloudflare Wrangler：已登录并验证成功
- Cloudflare Pages 项目名：`bobo-river-clash`

## 2. 提交到 GitHub

### 推荐方式

在项目根目录执行：

```bash
publish-github.bat 你的提交说明
```

这个脚本会依次执行：

1. `git add -A`
2. `git commit -m "你的提交说明"`
3. `git push origin main`

### 手动方式

```bash
git add -A
git commit -m "你的提交说明"
git push origin main
```

## 3. 部署到 Cloudflare

### 推荐方式

```bash
npm run deploy:cloudflare
```

或直接双击：

```text
deploy-cloudflare.bat
```

这个流程会：

1. 执行 `npm run build`
2. 将 `dist/` 发布到 Cloudflare Pages 项目 `bobo-river-clash`

## 4. 一次完整发布建议

如果你改了代码，建议顺序如下：

1. `npm run build`
2. 本地预览确认
3. `publish-github.bat 你的提交说明`
4. `npm run deploy:cloudflare`

## 5. 常见问题

### GitHub 推送失败

优先检查：

- `git remote -v` 是否仍然指向正确仓库
- 当前分支是否为 `main`
- 本机 GitHub 登录状态是否失效

### Cloudflare 部署失败

优先检查：

- `wrangler whoami` 是否仍然登录
- `wrangler.toml` 中的项目配置是否被改坏
- Cloudflare Pages 项目 `bobo-river-clash` 是否仍存在
- D1 绑定和环境变量是否仍正确

## 6. 当前可直接使用的命令

```bash
publish-github.bat 更新说明
npm run deploy:cloudflare
```
