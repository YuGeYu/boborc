# 啵啵小队出击维护说明

本文档面向后续维护者，按当前目录 `D:\document\html\boborc` 的实际代码整理。目标是让接手者快速定位核心模块、理解运行方式，并避免把本地产物和敏感配置一并带进仓库。

## 1. 项目概览

- 技术栈：Vue 3 + Phaser 3 + Express + Cloudflare Pages Functions + D1
- 项目形态：多页面前端小游戏，战斗内核运行在 Phaser 场景中
- 默认本地预览端口：`8080`
- 构建产物目录：`dist/`

当前主要模块分工如下：

- `src/data/gameContent.js`
  - 角色、装备、关卡、AI 对手、统一攻击冷却等静态数据
- `src/data/progression.js`
  - 本地账号、存档槽、默认进度、数据兼容
- `src/state/useGameState.js`
  - 页面和战斗共同使用的全局状态组合层
- `src/game/battleConfig.js`
  - 战斗配置标准化与结构兼容
- `src/game/scenes/PlayScene.js`
  - 战斗主逻辑、伤害结算、角色机制、敌方 AI
- `src/views/*.vue`
  - 页面 UI，分别对应大厅、战斗、装备、关卡、存档、设置、世界等页面
- `src/common/cloudApi.js`
  - 云存档与认证请求封装
- `functions/`
  - Cloudflare Pages Functions API
- `migrations/`
  - D1 数据库迁移脚本

## 2. 运行与发布

### 常用命令

- 安装依赖：`npm install`
- 本地开发：`npm run serve`
- 生产构建：`npm run build`
- 本地预览：`npm run preview`
- Cloudflare 部署：`npm run deploy:cloudflare`

### 辅助脚本

- `start-preview.bat`
  - 自动安装依赖、构建项目并启动本地预览
- `deploy-cloudflare.bat`
  - 构建后调用 `wrangler pages deploy dist`
- `scripts/sync-version.js`
  - 构建时同步 `version.txt`、`src/generated/buildMeta.js` 与 `public/build-info.json`

## 3. 环境变量与敏感信息

当前使用到的环境变量如下：

- `VUE_APP_API_URL`
- `VUE_APP_DEBUG_MODE`
- `VUE_APP_AI_CHAT_API_URL`
- `VUE_APP_AI_CHAT_API_KEY`

维护约定：

- AI 接口密钥只能放在环境变量中，不要再写回源码
- 若未配置 `VUE_APP_AI_CHAT_API_KEY`，前端应视为 AI 功能未启用
- 发布前检查 `.env.*` 是否包含不应公开的真实凭据
- 本地状态目录 `.wrangler/`、临时资源目录 `tmp_assets/` 不要提交

## 4. 前端核心入口

### 多页面入口

位于 `src/pages/`：

- `lobby.js`
- `battle.js`
- `shop.js`
- `equipment.js`
- `levels.js`
- `profile.js`
- `saves.js`
- `settings.js`
- `world.js`

这些入口分别挂载对应页面视图，页面层代码主要在 `src/views/`。

### Phaser 相关

- `src/components/Game.vue`
  - Vue 与 Phaser 的桥接层
- `src/game/game.js`
  - Phaser 启动入口
- `src/game/scenes/BootScene.js`
  - 资源与启动流程
- `src/game/scenes/PlayScene.js`
  - 战斗主逻辑
- `src/game/scenes/PauseScene.js` / `WinScene.js` / `LoseScene.js`
  - 暂停与结算相关场景

## 5. 重点维护区域

### 角色、装备、关卡数值

优先查看：

- `src/data/gameContent.js`
- `src/state/useGameState.js`
- `src/views/ShopPage.vue`
- `src/views/EquipmentPage.vue`
- `src/views/LevelsPage.vue`

如果只是改角色描述、装备文本或关卡展示，通常不需要直接改 Phaser 场景。

### 战斗逻辑与角色机制

优先查看：

- `src/game/scenes/PlayScene.js`
- `src/game/battleConfig.js`
- `src/components/Game.vue`

注意：

- 当前项目虽然已经兼容 `allies/enemies` 结构，但运行时核心仍偏向单主控角色模型
- 如果继续推进多己方单位，不建议继续堆叠新的全局 `player` 式变量
- 新增战斗事件后，要同步检查战斗页日志展示是否需要映射更新

### 本地存档与云存档

优先查看：

- `src/data/progression.js`
- `src/common/cloudApi.js`
- `src/state/useGameState.js`
- `functions/api/`
- `functions/_lib/`

排查云端问题时，先确认：

- 前端 `VUE_APP_API_URL` 是否正确
- Cloudflare Pages Functions 是否已部署
- D1 绑定与迁移是否一致

## 6. 仓库整理建议

当前适合保留在仓库中的内容：

- 源码目录 `src/`
- 公共资源 `public/`
- 后端函数 `functions/`
- 数据库迁移 `migrations/`
- 文档、脚本与配置文件

不建议提交的内容：

- `dist/`
- `node_modules/`
- `.wrangler/`
- `tmp_assets/`
- 本地数据库状态文件

## 7. 改动后检查清单

每次准备提交前，至少检查以下内容：

1. `npm run build` 是否成功
2. 新增配置是否写入 README 和本说明
3. 是否误提交了构建产物、缓存目录或密钥
4. 若改了云接口，是否同步检查 `functions/` 与前端请求参数
5. 若改了角色/装备/关卡逻辑，是否同步检查相关页面文案与战斗实际表现

## 8. 接手建议

首次接手时建议按下面顺序阅读：

1. `README.md`
2. `package.json`
3. `src/state/useGameState.js`
4. `src/data/gameContent.js`
5. `src/game/scenes/PlayScene.js`
6. `functions/api/` 与 `wrangler.toml`

如果目标是继续扩展玩法，优先保证“文档、配置、构建”三件事保持同步，不要让说明再次落后于代码。
